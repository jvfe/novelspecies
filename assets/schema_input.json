{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "https://raw.githubusercontent.com/nf-core/novelspecies/main/assets/schema_input.json",
    "title": "nf-core/novelspecies pipeline - params.input schema",
    "description": "Schema for the file provided with params.input",
    "type": "array",
    "items": {
        "type": "object",
        "properties": {
            "sample": {
                "type": "string",
                "pattern": "^\\S+$",
                "errorMessage": "Sample name must be provided and cannot contain spaces",
                "meta": ["id"]
            },
            "fasta": {
                "type": "string",
                "format": "file-path",
                "exists": true,
                "pattern": "^\\S+\\.(fasta|fa|fna|fas|seq)(\\.gz)?$",
                "errorMessage": "Genome assembly FASTA must be provided, cannot contain spaces and must have extension '.fasta', '.fa', '.fna', '.fas' or '.seq', optionally gzipped"
            },
            "genus": {
                "type": "string",
                "pattern": "^[A-Za-z][A-Za-z0-9_-]*$",
                "errorMessage": "Genus, if given, must be a single word without spaces",
                "meta": ["genus"]
            }
        },
        "required": ["sample", "fasta"]
    }
}
